// Copy this file to config.local.js (gitignored — never committed) and
// paste your deployed Apps Script Web App /exec URL below. Keeping the
// real URL out of committed source is a deliberate decision — see
// ../_prd/BUILD-PLAN.md -> Decisions (GitHub repo visibility).
window.STORAGE_BASE_CONFIG = {
  backendUrl: 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE'
};
